{
  "name": "tech-daily-weekly",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run check && npm run validate:content && astro build",
    "preview": "astro preview",
    "check": "astro check && tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "validate:content": "tsx src/cli/validate.ts",
    "generate:daily": "tsx src/cli/daily.ts",
    "generate:weekly": "tsx src/cli/weekly.ts",
    "scheduler:run": "tsx src/cli/scheduler.ts",
    "scheduler:install": "tsx src/cli/install-scheduler.ts",
    "container:start": "tsx src/cli/container.ts"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "astro": "^7.1.3",
    "fast-xml-parser": "^5.10.1",
    "gray-matter": "^4.0.3",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
